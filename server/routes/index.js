const seller = require("./seller")
const admin=require('./admin')


module.exports={seller, admin}



// we are using index.js file for routes because if we have multiple routes in future so that we can import export without writing messay code in index.js main file